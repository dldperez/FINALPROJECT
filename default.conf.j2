server {
    listen 80;

    location / {
        root /usr/share/nginx/html  
        #This is where the nginx gets the html files
        
        index home.html profile.html portfolio.html;
        #These are the html files for our portfolios.
    }
}
